/**
 * Constants for Secret Santa pairing generation algorithms
 */

/**
 * Maximum number of attempts for generating valid pairings
 * Used in all generation algorithms to prevent infinite loops
 */
export const MAX_GENERATION_ATTEMPTS = 1000
